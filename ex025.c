#include <stdlib.h>
#include <stdio.h>
#include <locale.h>
#include <string.h>

int main() {
    setlocale(LC_ALL, "Portuguese");
    float v_produto, preco, qnt, v_total;
    int i;

    for(i = 1; i <= 10; i++) {
        printf("Valor R$");
        scanf("%f", )
    }
    return 0;
}
